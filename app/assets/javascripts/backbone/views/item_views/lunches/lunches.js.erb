
define([
    'jquery',
    'underscore',
    'backbone',
    'handlebars',
    'marionette',
    '<%= javascript_path('backbone/views/item_views/lunches/lunches_template')%>'
], function($,_,Backbone,Handlebars,Marionette){

	var LunchesView = Backbone.Marionette.ItemView.extend({
		template: Handlebars.templates['lunches_template']
	});

	return LunchesView;

});