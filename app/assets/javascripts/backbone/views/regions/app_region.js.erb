define([
    'jquery',
    'underscore',
    'backbone',
    'handlebars',
    'marionette'
], function($,_,Backbone,handlebars,Marionette){

	var AppRegion = Backbone.Marionette.Region.extend({
		el: $('#app-body')
	});

	return AppRegion;

});
