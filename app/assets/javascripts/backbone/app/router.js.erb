// Filename: router.js
define([
    'jquery',
    'underscore',
    'backbone',
    'handlebars',
    'marionette'

], function($, _, Backbone,Handlebars,Marionette,MainApp,Global){

      var MainAppController = {

          Home: function() {    
            //var app_layout = new AppLayout();
            //var app_region = new AppRegion();

            //app_region.show(app_layout);

            console.log("works");        
          }

      };


      var Router = Backbone.Marionette.AppRouter.extend({
            appRoutes : {
                '': 'Home',
            }

      });

      return new Router({controller: MainAppController});

});